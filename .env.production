# Production Environment Configuration for R1 API

# Base URLs for R1 API
# Replace '<tablet-ip>' with the actual IP address of the tablet
VITE_R1_API_URL=http://<tablet-ip>/api
VITE_R1_WS_URL=ws://<tablet-ip>/ws

# Mode flag (optional, as Vite sets this automatically)
VITE_MODE=production 